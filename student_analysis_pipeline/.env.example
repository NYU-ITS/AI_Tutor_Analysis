# OpenWebUI Database (read-only access)
# format: postgresql://user:pass@host:port/dbname
DATABASE_URL=postgresql://user:password@localhost:5433/pilotgenai_dev_pg

# Pipeline Database (stores analysis results)
# format: postgresql://user:pass@host:port/dbname
PIPELINE_DATABASE_URL=postgresql://pipeline_user:password@localhost:5432/student_analysis

# Portkey LLM Gateway Configuration
PORTKEY_API_KEY=your_api_key_here
PORTKEY_BASE_URL=https://ai-gateway.apps.cloud.rt.nyu.edu/v1

# Optional: pytest live/unit model override (short name e.g. gpt-4o, or full Portkey id)
# LLM_TEST_MODEL=gpt-4o
